# personal-website

Personal website, built with plain HTML/CSS/JS and deployed via GitHub Pages.

## Local preview

Open `index.html` directly in a browser, or serve it locally:

```
python3 -m http.server 8000
```

Then visit http://localhost:8000
